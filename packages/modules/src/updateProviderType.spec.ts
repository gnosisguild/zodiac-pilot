import { describe, it } from 'vitest'

describe('updateProviderType', () => {
  it.todo('updates the starting point to EOA when an injected provider is used')
  it.todo('updates the starting point to SAFE if anything else is used')
})
