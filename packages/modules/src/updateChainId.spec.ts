import { describe, it } from 'vitest'

describe('updateChainId', () => {
  describe('Avatar', () => {
    it.todo('Updates the chain of the avatar')
  })

  describe('Waypoints', () => {
    it.todo('updates the chain of all modules')
    it.todo('updates the chain of the endpoint')
  })
})
