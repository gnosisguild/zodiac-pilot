fragment RoleListEntry on Role {
  id
  key
  lastUpdate
}

query FetchRoles($account: String!) {
  rolesModifiers(avatar: $account) {
    avatar
    chainId
    roles {
      ...RoleListEntry
    }
  }
}
