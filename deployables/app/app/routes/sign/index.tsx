import { Info } from '@zodiac/ui'

const SubmitIndex = () => (
  <Info title="Record transactions">
    Use the Pilot extension to record transactions and then submit them as a
    batch.
  </Info>
)

export default SubmitIndex
